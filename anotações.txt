
type ContaCorrente struct {
	Titular       string
	NumeroAgencia int
	NumeroConta   int
	Saldo         float64
}

// func main() {
// 	var titular string = "Gustavo Bispo"
// 	var numeroAgencia int = 589
// 	var numeroConta int = 123456
// 	var saldo float64 = 1000.50

// 	fmt.Println(titular, numeroAgencia, numeroConta, saldo)

// fmt.Printf("Titular: %s\n", titular)
// fmt.Printf("Número da Agência: %d\n", numeroAgencia)
// fmt.Printf("Número da Conta: %d\n", numeroConta)
// fmt.Printf("Saldo: %.2f\n", saldo)

contaCliente := ContaCorrente{Titular: "Gustavo Bispo", NumeroAgencia: 589,
// 		NumeroConta: 123456, Saldo: 1000.50}

// 	ContaCliente2 := ContaCorrente{"Maria Silva", 123, 654321, 2500.75}

// 	fmt.Println(contaCliente)
// 	fmt.Println(ContaCliente2)

// 	// Criando uma instância da struct ContaCorrente usando ponteiro = * e new()
// 	// O operador new() aloca memória para a struct e retorna um ponteiro para ela.
// 	// O operador * é usado para declarar uma variável como um ponteiro para a struct.
// 	var contaCliente3 *ContaCorrente
// 	contaCliente3 = new(ContaCorrente)
// 	contaCliente3.Titular = "João Souza"
// 	contaCliente3.NumeroAgencia = 456
// 	contaCliente3.NumeroConta = 987654
// 	contaCliente3.Saldo = 1500.25

// 	var contaCliente4 *ContaCorrente
// 	contaCliente4 = new(ContaCorrente)
// 	contaCliente4.Titular = "João Souza"
// 	contaCliente4.NumeroAgencia = 456
// 	contaCliente4.NumeroConta = 987654
// 	contaCliente4.Saldo = 1500.25

// 	fmt.Println(contaCliente3)
// 	fmt.Println(contaCliente4)

// 	// Comparando as contas:
// 	// A comparação de structs em Go é feita comparando os valores dos campos.
// 	// Se todos os campos forem iguais, as structs são consideradas iguais.

// 	fmt.Println(*ContaCliente3 == *contaClient4)

// }



----------------------------------------------------------------------------------------------------------

func (c *ContaCorrente) Sacar(valorSaque float64) string {
	podeSacar := valorSaque > 0 && valorSaque <= c.Saldo
	if podeSacar {
		c.Saldo -= valorSaque
		return "Saque realizado com sucesso!"
	} else {
		return "Saldo insuficiente!"
	}
}

func (c *ContaCorrente) Depositar(valorDeposito float64) (string, float64) {
	if valorDeposito > 0 {
		c.Saldo += valorDeposito
		return "Depósito realizado com sucesso!", c.Saldo
	} else {
		return "Valor de depósito inválido!", c.Saldo
	}

}

func main() {
	ContaCliente := ContaCorrente{}
	ContaCliente.Titular = "Usuario"
	ContaCliente.NumeroAgencia = 123
	ContaCliente.NumeroConta = 456
	ContaCliente.Saldo = 1000.00

	fmt.Println(ContaCliente.Saldo)
	status, valor := ContaCliente.Depositar(200.00)
	fmt.Println(status)
	fmt.Println(valor)
	// fmt.Println(ContaCliente.Saldo)

	// valorSaque := 50.00
	// mensagem := ContaCliente.Sacar(valorSaque)
	// fmt.Println(mensagem)

	// fmt.Println(ContaCliente.Saldo)

	// valorDeposito := 200.00
	// ContaCliente.Depositar(valorDeposito)
	// fmt.Println(ContaCliente.Saldo)
